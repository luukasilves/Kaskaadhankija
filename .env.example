# Kaskaadhankija — configuration.
#
# Copy to `.env.local` for local work. On Fly, the non-secret values live in
# fly.toml's [env] block and the SMTP credentials in `fly secrets set`.
# Everything here has a working default except SMTP, which is optional.

# ---------------------------------------------------------------- database
# One SQLite file. In the container this points at the mounted volume.
DATABASE_PATH=./data/kaskaadhankija.db

# ---------------------------------------------------------------- test harness
# The single switch for the test deployment. With DEMO_MODE on, the opening
# screen offers personas, a strip above the application switches between them
# and moves a virtual clock, and the sample data can be reset. With it unset
# there are no personas, no strip and no clock — and the demo-only server
# actions refuse to run — so a production deployment simply leaves it out.
DEMO_MODE=1

# Used in notification links. Set it to the real origin in a deployment.
APP_BASE_URL=http://localhost:3000

# ---------------------------------------------------------------- email
# Optional. Without SMTP_HOST the in-app notification log is the only channel,
# which is what the test deployment runs on. With it, every notification is
# also emailed, and each notification row records whether the send succeeded.
# SMTP_HOST=smtp.example.ee
# SMTP_PORT=587
# SMTP_SECURE=0
# SMTP_USER=
# SMTP_PASS=
# EMAIL_FROM=Kaskaadhankija <tellimused@example.ee>

# Where the buyer team's summaries go. Empty falls back to the active admin
# users' addresses.
# TEAM_NOTIFICATIONS_EMAIL=tellimused@example.ee

# Who may receive real mail: addresses and whole domains, comma-separated
# (`@agenticstate.org, keegi@riik.ee`). With DEMO_MODE on, anything else is
# recorded as "suppressed" and never attempted — the seeded partners have
# fictional addresses — and an empty list means nothing is sent at all. Without
# DEMO_MODE an empty list allows everyone. `*` opens it explicitly.
# EMAIL_ALLOWED_RECIPIENTS=@example.ee
# SMTP_TIMEOUT_MS=15000

# Print the emails to the log instead of sending them, even when SMTP is
# configured — useful when developing the notification copy.
# EMAIL_DEV_MODE=1

# ---------------------------------------------------------------- sign-in
# Signs one-time sign-in codes and session tokens. Required for sign-in outside
# DEMO_MODE. On Fly the deploy workflow generates it once; locally use
# `openssl rand -hex 32`.
# AUTH_SECRET=

# Domains whose addresses may sign in as buyer admins without being added to
# the team first, e.g. `@riigikantselei.ee`. The user row is created when a
# code is verified, never when one is requested, and the rule never reactivates
# somebody an admin switched off. Empty means only listed people can sign in.
#
# Convenient while the buyer organisation is testing; it also means anyone
# holding a mailbox at that domain can publish rounds and confirm allocations,
# which is irreversible — see [L-08] before using it for a real procurement.
# The address still has to pass EMAIL_ALLOWED_RECIPIENTS for the code to be
# delivered in a DEMO_MODE environment.
# AUTO_ADMIN_EMAIL_DOMAINS=@riigikantselei.ee

# ---------------------------------------------------------------- seed
# The buyer persona the sample data creates, and the label that appears in the
# audit log next to everything it did.
SEED_ADMIN_NAME=Mari Tamm
SEED_ADMIN_EMAIL=mari.tamm@naidis.riigikantselei.ee

# Real representatives layered over the fictional sample list on every seed, so
# a reset of the test environment does not wipe the team's sign-ins. Entries
# `registrikood,nimi,e-post[,roll]` separated by `;`. Keep it in the
# deployment's secrets, not in the repository.
# SEED_REPRESENTATIVES=10000001,Mari Maasikas,mari@example.ee;80000003,Jaak Juurikas,jaak@example.ee,asendaja

# The buyer team's real members, added on every seed for the same reason:
# `nimi,e-post[,roll]` separated by `;`, admin unless the role says `liige`.
# These are added *alongside* the fictional SEED_ADMIN persona, not instead of
# it, so the sample scenarios still read as the specialists reviewed them.
# SEED_TEAM=Luukas Ilves,luukas@example.org;Kirke Kask,kirke@example.org,liige
